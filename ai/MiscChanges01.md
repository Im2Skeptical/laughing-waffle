
Below are a list of mostly distinct patches/changes/feature updates. Please implement each; if intervention is required you can pause that task, commit your progress if any to the git with the description outlining any neccessary requests or confirmation you need before proceeding.

After completing each task, commit it to the git with a sensible title and description.

After reaching the end of the list, roughly summarize changes and restate any incomplete tasks that require additional intervention. 


-

Event Log gating

Lets make the Event Log also gated by memory skill tree node, I think can be a simple visibility toggle but please investigate. 

-
Event Card timing

For the event cards I want the border to have a color that 'drains down' as an indicator of the time till expiry. 

-

Process Widget

The buttons to expand the drawers that contain the routing controls need to actually work as buttons. Currently it works by a little text arrow which is very hard to hit. Instead have a rectangle button and when the tray expands just move the button to the left/right of the tray. The little arrow can be kept as an indicator.

-

Consume button 

When clicking an object to consume like the skill tomes, should hover a button over the item asking consume? Have that button fade out with some time- essentially this is a visual 'double-click' or 'double tap'.

-

Gameboard Header Depth
Currently the titlecards region and hub can go over gampieces and effects - notably the region one can cover envEvents and their fade off animation. Please have gamepieces and effects always on top these cards.